import { test } from '@playwright/test';

test.describe('06 Subtasks', () => {
  test.skip(true, 'Requires authenticated fixture and seeded todo setup helper.');

  test('add subtasks and verify progress', async () => {});
  test('delete subtask', async () => {});
});
