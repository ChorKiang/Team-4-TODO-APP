import { test } from '@playwright/test';

test.describe('11 Calendar', () => {
  test.skip(true, 'Requires seeded holidays and due-date fixtures for deterministic assertions.');

  test('load calendar and navigate months', async () => {});
  test('open day modal', async () => {});
});
