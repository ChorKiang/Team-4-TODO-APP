import { test } from '@playwright/test';

test.describe('07 Tags', () => {
  test.skip(true, 'Requires authenticated fixture and modal interaction helpers.');

  test('create and edit tags', async () => {});
  test('filter by tag badge', async () => {});
});
