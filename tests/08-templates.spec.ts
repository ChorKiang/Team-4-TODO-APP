import { test } from '@playwright/test';

test.describe('08 Templates', () => {
  test.skip(true, 'Requires fixture setup for template source todo and subtasks.');

  test('save as template and use template', async () => {});
  test('edit and delete template', async () => {});
});
